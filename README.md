Periodically searches Vinted for new items with set parameters (preset list and params index.js).
Checks whether items are new or not by comparing to previously found items. 
If items are new, email is sent. Email details must be set in `env.json` before this is functional.
